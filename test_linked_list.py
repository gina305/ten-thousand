"""
- Handle rolling dice
- Add `roll_dice` static method to GameLogic class.
- The input to `roll_dice` is an integer between 1 and 6.
- The output of `roll_dice` is a tuple with random values between 1 and 6.
- The length of tuple must match the argument given to `roll_dice` method.

"""
import pytest
from ten_thousand.game_logic import GameLogic


#@pytest.mark.skip("pending")
def check_length_of_k():
    # values = GameLogic.roll_dice(1)
    # assert len(values) == 1
    # value = values[0]
    # assert 1 <= value <= 6


def check_k_equals_list_length():
    # values = GameLogic.roll_dice(1)
    # assert len(values) == 1
    # value = values[0]
    # assert 1 <= value <= 6



def check_k_is_not_posiive():
    # values = GameLogic.roll_dice(1)
    # assert len(values) == 1
    # value = values[0]
    # assert 1 <= value <= 6



def list_size_equals_1(): # Is this length or integer??
    # values = GameLogic.roll_dice(1)
    # assert len(values) == 1
    # value = values[0]
    # assert 1 <= value <= 6

def k_in_middle():
    # values = GameLogic.roll_dice(1)
    # assert len(values) == 1
    # value = values[0]
    # assert 1 <= value <= 6




